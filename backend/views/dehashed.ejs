<!DOCTYPE html>
<html>
  <head>
    <title>Dehashed</title>
  </head>
  <body>
    <h1>Dehashed Results</h1>
    <% if(data) { %>
      <!-- Display data here -->
      <!-- Details would depend on the structure of data returned from Dehashed API -->
    <% } else { %>
      <p>No data available</p>
    <% } %>
  </body>
</html>
